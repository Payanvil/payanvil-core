package com.wallet.transfer.domain;

/**
 * Кто оплачивает сетевую комиссию (TRX) за перевод USDT.
 * <p>
 * SENDER — комиссия с отправителя, получатель получает полную сумму
 * (безопасное поведение по умолчанию). RECIPIENT — комиссия в пересчёте
 * на USDT вычитается из суммы перевода, получатель получает меньше.
 */
public enum FeePayer {
    SENDER,
    RECIPIENT
}
